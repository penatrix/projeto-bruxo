# Banco
DB_ROOT_PASSWORD=troque-esta-senha-root
DB_NAME=otserver
DB_USER=otserver
DB_PASSWORD=troque-esta-senha
DB_PORT=3306

# Servidor
SERVER_NAME=Vallenmoor
SERVER_IP=127.0.0.1
# Aspas importam: sem elas o valor quebra ao dar `source .env` no shell.
SERVER_MOTD="Bem-vindo ao servidor de desenvolvimento."

# Opcional: caminho do binário, se o build.sh não achar sozinho
# TFS_BINARY=/srv/build/tfs
